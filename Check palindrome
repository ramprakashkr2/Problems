#include<stdio.h>
int main()
{
    char s[100];
    scanf("%s",s);
    int f=0;
    for(int i=0;i<strlen(s);i++)
    {
        f=f^(1<<(s[i]-'a'));
    }
    f=f&(f-1);
    if(f==0) printf("yes");
    else printf("no");
}
